inherit_from: .rubocop_todo.yml

require:
  - rubocop-jekyll
  - rubocop-performance

inherit_gem:
  rubocop-jekyll: .rubocop.yml

AllCops:
  Exclude:
    - vendor/**/*

Metrics/BlockLength:
  Exclude:
    - spec/**/*
